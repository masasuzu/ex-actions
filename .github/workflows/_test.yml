on:
  workflow_call:
    inputs:
      ENV:
        type: string
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: 'read'
      id-token: 'write'

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: 
        if: ${{ inputs.ENV == 'prd' }}
        run: echo true
